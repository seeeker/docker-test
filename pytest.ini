[pytest]
# Add the project root to sys.path so tests can `import app` (app.py lives here,
# but the test files live under tests/). Without this, pytest only puts the
# tests/ dir on the path and the import fails.
pythonpath = .

# Where to look for tests.
testpaths = tests
